#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    char ch;
    cin >> ch;
    if (ch == 'B')
        cout << "Y\n";
    else if (ch == 'Y')
        cout << "R\n";
    else cout << "B\n";
    return 0;
}
