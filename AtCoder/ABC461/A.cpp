#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int a, d;
    cin >> a >> d;
    cout << (a <= d ? "Yes" : "No") << "\n";
    return 0;
}
