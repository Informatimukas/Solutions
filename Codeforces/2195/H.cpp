#include <bits/stdc++.h>
using namespace std;

using ii = pair<int, int>;
using triangle = array<ii, 3>;

vector<triangle> collectEven(int n) {
    for (int i = 1; i <= 3 * n; i += 2)

}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    int tot = 3 * n * n;
    if (n % 2)
    vector<triangle> res;
    return 0;
}
