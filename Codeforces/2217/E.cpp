#include <bits/stdc++.h>
using namespace std;

vector<int> Solve(const vector<int>& p, const vector<int>& d) {
    vector<int> q(p.size());

}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int T;
    cin >> T;
    while (T--) {
        int n;
        cin >> n;
        vector<int> p(n);
        for (auto& x : p)
            cin >> x;
        vector<int> d(n);
        for (auto& x : d)
            cin >> x;

    }
    return 0;
}
